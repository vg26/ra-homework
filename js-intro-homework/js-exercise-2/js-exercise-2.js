let radius = 10
area = Math.PI*(radius*radius)
console.log(`The area of a circle with radius of ${radius} is: ${area.toFixed(2)}`)

console.log('--------------------------------------')

let circleCircumference = 100
radius = circleCircumference / (2*Math.PI)
console.log(`The circle has a circumference of ${circleCircumference}. Its radius is ${radius.toFixed(1)}`)

console.log('--------------------------------------')

diametar = radius.toFixed(1)*2
console.log(`The circle has a circumference of ${circleCircumference}m. Its radius is ${radius.toFixed(1)} and the diametar is ${diametar}`) 










